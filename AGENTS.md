# Renaiss Collector Bot — 범용 AI 작업 기준

> 이 문서는 Codex를 포함한 모든 AI 작업자가 따르는 `renaiss-tcg-bot` 저장소의 최상위 작업 규칙이다.
> 인접한 `pokemon-bot`의 `CLAUDE.md`와 `GAME_PHILOSOPHY.md`는 역사적 참고 자료이며,
> 이 저장소의 명령어·UX·제품 결정을 구속하지 않는다.

## 제품 정의

Renaiss Collector Bot은 영어권 Telegram 그룹에서 돌아가는 소셜 TCG 시장 학습 게임이다.

한 줄 약속:

> Catch for luck. Predict and pick with insight. Learn the market through Renaiss.

목표 우선순위는 다음과 같다.

1. 공개 그룹의 참여와 대화
2. Renaiss 가격 데이터의 반복 사용과 이해
3. 신규·기존 유저가 함께 노는 공정한 수집 경험
4. 공유 가능한 운·안목의 순간
5. 검증 가능한 Daily Market Pick과 다음 날 결과

## 소스 오브 트루스

판단이 충돌하면 아래 순서로 따른다.

1. 현재 대화에서 사용자가 확정한 결정
2. 이 `AGENTS.md`
3. `renaiss_bot/docs/GAME_PHILOSOPHY.md`
4. 날짜가 가장 최신인 Renaiss 실행 기획
5. 이전 Renaiss 기획과 현재 코드
6. Pokémon/TGPoke 문서와 코드는 참고 자료

과거 문서의 미확정안보다 사용자가 나중에 확정한 결정이 우선한다.

## 절대 규칙

1. **사용자 노출 문구와 명령은 영어다.**
   - Telegram 메시지, 버튼, 명령 도움말, 오류 문구는 영어로 작성한다.
   - 내부 주석과 개발 문서는 한국어를 사용할 수 있다.

2. **공식 포획 키는 `c`다.**
   - `c`는 catch의 한 글자 진입점이다.
   - Pokémon-bot의 `ㅌ/ㅊ` 명령 체계를 르네이스에 강제하지 않는다.
   - 레거시 `d/f` 드랍을 코어 루프로 병행하지 않는다.

3. **공개 그룹이 무대다.**
   - 스폰, 포획 추첨, 가격 추측, 가격 공개, 큰 성과와 챔피언은 그룹에서 보인다.
   - DM은 Daily Pick 선택·개인 기록처럼 그룹을 시끄럽게 만드는 행동에 사용한다.
   - 핵심 재미가 외부 웹사이트에 의존하면 안 된다.

4. **입구는 설명 없이 한 번이다.**
   - 캐주얼 유저는 평생 `c`만 눌러도 수집과 공개의 재미를 느껴야 한다.
   - 가격 추측과 Daily Pick은 자연스럽게 열리는 선택 레이어다.
   - 첫 화면에 별도 경제나 거래 규칙을 만들지 않는다.

5. **FMV는 포획 전에 숨긴다.**
   - 카드명·세트·언어·등급은 보여줄 수 있다.
   - Renaiss FMV와 common/rare/grail 같은 가격 밴드는 추측 전에 노출하지 않는다.
   - 공개 시 가격, 출처, 신뢰 상태를 함께 보여준다.

6. **획득은 운, 선택은 안목이다.**
   - 공개 포획 당첨은 누적 자산이나 과금이 아닌 랜덤으로 결정한다.
   - 가격 추측과 하루 한 장의 Daily Pick에서 카드 지식이 실력이 된다.
   - 오래 플레이해 쌓은 돈이나 능력치가 경쟁 우위가 되면 안 된다.

7. **영구 컬렉션과 Daily Pick 기록을 분리한다.**
   - 획득 카드는 수집·Showcase·Flex용이다.
   - Daily Pick은 그날 공개된 검증 카드 중 한 장을 고르는 별도 안목 기록이다.
   - 경쟁용 현금, 포지션, 매수·매도, 수수료 시스템을 만들지 않는다.
   - 컬렉션 FMV와 판매금은 Daily Pick 결과나 점수에 합산하지 않는다.

8. **돈 버는 게임으로 만들지 않는다.**
   - 현금 잔액·가상 투자금·수익 보장·실물 소유권을 제공하지 않는다.
   - 매수·매도, 수수료, short, leverage, P2P 현금화, 투자 권유 문구는 범위 밖이다.
   - 외부 표현은 `Collector Market Literacy`와 참고용 시장 데이터에 맞춘다.

9. **Renaiss 데이터가 판정자다.**
   - exact identity, freshness, confidence, source 조건을 통과한 가격만 경쟁에 사용한다.
   - 불확실한 가격은 수집용으로 남기고 랭킹·정산에서 제외한다.
   - 모든 금액은 experimental reference data이며 투자 조언이 아니다.

10. **자랑의 대상은 재산이 아니라 운과 안목이다.**
    - 좋은 공유: 희귀 포획, 정확한 가격 추측, 언더독 Daily Pick, 다음 날 결과, Showcase 10.
    - 나쁜 공유: 누적 자산으로 줄 세우기, 현금 수익 과장, 고인물의 영구 지배.

11. **놓친 벌과 메시지 도배를 만들지 않는다.**
    - 참여하지 않아도 자산·능력치가 영구적으로 뒤처지지 않는다.
    - 자동 알림과 Flex는 빈도 제한을 두고, 그룹 대화를 압도하지 않는다.

12. **Pokémon-bot은 재사용 자료이지 제품 규칙이 아니다.**
    - 검증된 렌더러·DB 패턴·소셜 메커닉은 재사용할 수 있다.
    - 한국어 명령, 포켓몬 세계관, 이전 시즌 경제, 과거 절대 규칙은 자동 이식하지 않는다.

## 개발 규칙

- 기능 변경 전 `renaiss_bot/docs/GAME_PHILOSOPHY.md`와 최신 실행 기획을 확인한다.
- 버그는 가능하면 재현 테스트를 먼저 추가하고 수정한다.
- 변경 후 `python -m pytest tests -q`와 `git diff --check`를 실행한다.
- 사용자 요청 없이 배포·푸시·머지·봇 재시작을 하지 않는다.
- 기존 테이블을 DROP하지 않는다. 스키마 변경은 호환 가능한 추가 마이그레이션으로 한다.
- 스케줄과 일일/주간 경계는 KST(`Asia/Seoul`)를 명시적으로 사용한다.
- Renaiss API 실패·429·가격 누락은 게임 전체 장애가 되지 않게 폴백 또는 제외 처리한다.
- 사용자 작업물과 미추적 파일을 임의로 삭제하거나 덮어쓰지 않는다.

## 기능 추가 전 체크리스트

- [ ] `c`만 누르는 유저도 여전히 재미있는가?
- [ ] 그룹에 대화·반응·자랑할 순간이 생기는가?
- [ ] 신규 유저가 누적 자산·과거 선택 횟수 때문에 불리하지 않은가?
- [ ] Renaiss 데이터가 장식이 아니라 실제 판단에 쓰이는가?
- [ ] 가격 공개 전 정답이나 가치 밴드가 새지 않는가?
- [ ] 외부 링크 없이도 핵심 경험이 완결되는가?
- [ ] 돈벌이·투자 권유로 오해될 문구가 없는가?
- [ ] 자동 메시지 빈도가 그룹에 부담을 주지 않는가?

하나라도 아니면 기능을 단순화하거나 사이드 레이어로 내린다.

## 완료 전 의무 반박·위험 감사

기능 구현이 끝났다고 바로 완료 보고하지 않는다. 마지막에 반드시 아래 순서로 현재 기획과 구현을 공격적으로 다시 검토한다.

1. **기획 반박**
   - 이 기능이 없어도 핵심 목표를 달성할 수 있는가?
   - 기능이 캐주얼 유저에게 설명서·숙제·누적 격차를 만들지 않는가?
   - 공모전·파트너 노출을 위해 실제 사용자 재미를 희생하지 않았는가?
   - 문서의 가정과 현재 데이터/API/운영 여건이 실제로 맞는가?

2. **더 나은 방법 비교**
   - 더 작은 기능으로 같은 가설을 검증할 방법을 최소 1개 제시한다.
   - 현재안과 대안을 복잡도, 사용자 가치, 측정 가능성, 운영 위험으로 비교한다.
   - 대안이 더 낫다면 이미 만든 기능에 매몰되지 말고 전환을 권고한다.

3. **심각 이슈 감사**
   - 데이터 정확성·가격 freshness·API 한도와 장애 폴백
   - 중복 처리·동시성·재시작 복구·거래 원자성
   - 신규 첫 경험·그룹 노이즈·고인물 격차·돈벌이 오해
   - 이벤트 계측·성공 지표 측정 가능성·운영자 가시성
   - DB 마이그레이션·실환경 경로·배포 전 검증 누락

4. **심각도 분류**
   - `P0`: 배포·파일럿 전에 반드시 해결. 데이터 손상, 잘못된 정산, 핵심 루프 불능, 성공 여부 측정 불가.
   - `P1`: 파일럿 품질이나 리텐션을 크게 훼손. 빠른 후속 수정 필요.
   - `P2`: 개선 사항. 핵심 검증을 막지는 않음.

5. **완료 보고 형식**
   - 구현 결과와 테스트
   - 반박 및 더 나은 대안
   - 발견한 P0/P1/P2 이슈
   - 지금 배포 가능한지에 대한 명시적 판단
   - 다음 권장 순서

심각한 문제가 있으면 숨기거나 “다음 단계”로 뭉개지 않는다. 사용자가 수정까지 요청하지 않은 리뷰 작업에서는 임의로 범위를 넓혀 고치지 말고, 근거와 권고를 먼저 제시한다.
